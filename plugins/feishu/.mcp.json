{
  "mcpServers": {
    "feishu-mcp": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "./scripts/feishu_http_mcp.py"
      ],
      "note": "Stable local Feishu MCP server backed by direct Feishu OpenAPI HTTP calls. Uses FEISHU_USER_ACCESS_TOKEN for user-mode APIs and tenant_access_token only when a tool explicitly needs app identity."
    }
  }
}
